# Copy to .env (or .env.local) and adjust if the contact form should post
# somewhere other than the default production endpoint. Optional - if unset,
# the app falls back to https://absoluteranking.com/ppc/services/google-ads-management/contact.php
VITE_CONTACT_API_URL=https://absoluteranking.com/ppc/services/google-ads-management/contact.php
